# Caminho para o banco de dados via mem�ria
spring.datasource.url = jdbc:h2:mem:reservaquartos
# Classe respons�vel pela comunica��o com o banco de dados
spring.datasource.driver-class-name = org.h2.Driver
# Usu�rio do banco de dados
spring.datasource.username = sa
# Senha do banco de dados
spring.datasource.password =

# Dialeto para convers�o do JPA para o SQL
spring.jpa.database-platform = org.hibernate.dialect.H2Dialect
# Propriedade respons�vel para cria��o/atualiza��o de tabelas
spring.jpa.hibernate.ddl-auto=update

# Propriedade para visualizar os dados do H2
spring.h2.console.enabled=true
